name: Notify Discord on Push

on:
  push:
    branches:
      - "*"  # hoặc ghi rõ tên branch như 'main', 'master'

jobs:
  notify_discord:
    runs-on: ubuntu-latest
    steps:
      - name: Gửi thông báo lên Discord
        run: |
          TIMESTAMP=$(date +"%d/%m/%Y %H:%M:%S")
          curl -H "Content-Type: application/json" \
          -X POST \
          -d @- ${{ secrets.DISCORD_WEBHOOK }} <<EOF
          {
            "content": "📢 **Commit mới được đẩy lên GitHub!**\n\n📁 **Repo:** ${{ github.repository }}\n🌿 **Nhánh:** ${{ github.ref_name }}\n👤 **Tác giả:** ${{ github.event.head_commit.author.name }}\n📝 **Nội dung:** ${{ github.event.head_commit.message }}\n🕒 **Thời gian:** ${TIMESTAMP}\n🔗 **Link:** ${{ github.event.head_commit.url }}"
          }
          EOF
